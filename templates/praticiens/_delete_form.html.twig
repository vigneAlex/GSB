<form method="post" action="{{ path('praticiens_delete', {'id': praticien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ praticien.id) }}">
    <button class="btn">Delete</button>
</form>
