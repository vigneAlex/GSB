<form method="post" action="{{ path('visites_delete', {'id': visite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visite.id) }}">
    <button class="btn">Delete</button>
</form>
