<form method="post" action="{{ path('motifs_delete', {'id': motif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motif.id) }}">
    <button class="btn">Delete</button>
</form>
